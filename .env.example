# Copy to .env (already gitignored — never commit real secrets here).
#
# Optional. Without it, marchi starts locked (NFR-SC-01) and stays
# locked until someone opens the web UI and unlocks it by hand — every
# `docker compose restart` otherwise needs that same manual step.
MARCHI_MASTER_KEY=
